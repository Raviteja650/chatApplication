package com.example.chatApplication.Model;

public enum MessageType {
    CHAT,  // when user send the message

    JOIN, // when user joins the chat

    LEAVE  // when user leaves the chat or session
}
